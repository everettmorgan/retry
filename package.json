{
  "name": "ejmorgan-retry",
  "version": "3.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "license": "MIT",
  "description": "Production-grade retry with exponential backoff, jitter, abort signals, and timeouts.",
  "repository": "git@github.com:everettmorgan/retry.git",
  "author": "Everett Morgan",
  "engines": {
    "node": ">=22"
  },
  "keywords": [
    "retry",
    "backoff",
    "exponential",
    "jitter",
    "abort",
    "timeout",
    "resilience"
  ],
  "scripts": {
    "build": "tsc",
    "test": "yarn linter && yarn tests",
    "linter": "eslint --ext .ts src/",
    "tests": "env TS_NODE_COMPILER_OPTIONS='{\"module\": \"commonjs\" }' nyc --reporter=html --reporter=text mocha -r ts-node/register 'tests/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/chai": "^4.2.22",
    "@types/mocha": "^9.0.0",
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^5.4.0",
    "@typescript-eslint/parser": "^5.4.0",
    "chai": "^4.3.4",
    "eslint": "^8.3.0",
    "mocha": "^9.1.3",
    "nyc": "^15.1.0",
    "ts-node": "^10.4.0",
    "typescript": "^5.7.0"
  }
}
